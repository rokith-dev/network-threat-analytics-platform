# Network Threat Analytics Platform

A starter Python project for packet sniffing, threat detection, dashboarding, and alerting.

## Structure
- `app/` core application modules
- `data/` local logs and exports
- `tests/` test placeholders
- `docker/` container setup files
- `docs/` design and operational notes
